# Migrated from dev-docs/hledger-compatibility.md's "In Scope (v1)" table
# (09-compatibility-system.md §9.5 — transcription-with-verification, not new
# analysis). status: proposed — not yet executable-verified against a real
# hledger 1.52.4 binary; that is compat-differential-tester's job.

id: LK-COMPAT-CHECKORDEREDDATES-001
area: checks.ordereddates
kind: compatible

upstream:
  project: hledger
  compatibility_target: "1.52.4"

ledgerkit:
  relationship: equivalent

compatibility:
  upstream_syntax_preserved: true
  extension_requires_explicit_syntax: n/a

reason: >
  hledger's `ordereddates` check confirms transactions appear in
  non-decreasing date order. Ledgerkit's `check_ordereddates`
  implements the identical rule, invoked by name (`check ordereddates`).

implementation:
  - ledgerkit/checks.py:check_ordereddates

tests:
  - tests/test_checks/test_checks.py::TestCheckOrdereddates

evidence:
  - kind: manual
    ref: "https://hledger.org/1.52/hledger.html#check"
    pinned_at: "1.52.4"

status: proposed
verified_by: null
verified_date: null
